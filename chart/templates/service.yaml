apiVersion: v1
kind: Service
metadata:
  name: {{ include "thinkus-microservicio.fullname" . }}
  labels:
    {{- include "thinkus-microservicio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thinkus-microservicio.selectorLabels" . | nindent 4 }}
